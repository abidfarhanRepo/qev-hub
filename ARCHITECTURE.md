# QEV Hub - Charging Stations System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                    (Next.js Web Application)                     │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      /charging Page                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Google Maps                                                │ │
│  │  - Interactive Map                                          │ │
│  │  - Station Markers (Green/Red)                             │ │
│  │  - Info Windows                                            │ │
│  │  - User Location                                           │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Filters: [All] [Available] [Nearby]                       │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Station Cards Grid                                         │ │
│  │  - Name, Address                                           │ │
│  │  - Charger specs                                           │ │
│  │  - Availability                                            │ │
│  │  - Amenities                                               │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPABASE CLIENT                               │
│                  (src/lib/supabase.ts)                          │
│                                                                  │
│  - Authentication                                                │
│  - Database Queries                                              │
│  - Real-time Subscriptions                                       │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    SUPABASE DATABASE                             │
│                      (PostgreSQL)                                │
│  ┌─────────────────────────────────────────────────────────────┤
│  │  charging_stations                                           │
│  │  - id, name, address                                        │
│  │  - latitude, longitude                                      │
│  │  - charger_type, power_output_kw                           │
│  │  - total_chargers, available_chargers                      │
│  │  - status, amenities                                       │
│  │  - RLS: Public read access                                 │
│  ├─────────────────────────────────────────────────────────────┤
│  │  charging_sessions                                           │
│  │  - id, user_id, station_id, vehicle_id                     │
│  │  - start_time, end_time                                    │
│  │  - energy_delivered_kwh, cost_qar                          │
│  │  - status, notes                                           │
│  │  - RLS: User-specific access                              │
│  └─────────────────────────────────────────────────────────────┘
└─────────────────────────────────────────────────────────────────┘
                                ▲
                                │
                    ┌───────────┴───────────┐
                    │                       │
                    ▼                       ▼
┌──────────────────────────────┐ ┌──────────────────────────────┐
│   DATA SYNC SERVICE          │ │   API ENDPOINT               │
│                              │ │                              │
│  src/services/               │ │  src/app/api/                │
│  charging-sync.ts            │ │  sync-stations/route.ts      │
│                              │ │                              │
│  - Periodic sync             │ │  GET: Check sync status      │
│  - Batch updates             │ │  POST: Trigger manual sync   │
│  - Error handling            │ │                              │
└──────────────────────────────┘ └──────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│               TARSHEED DATA SCRAPER                              │
│            (src/lib/tarsheed-scraper.ts)                        │
│                                                                  │
│  Current: Mock data (3 KAHRAMAA stations)                       │
│  Future:  Real Tarsheed API integration                         │
│                                                                  │
│  Functions:                                                      │
│  - scrapeTarsheedData()                                         │
│  - calculateDistance()                                          │
│  - fetchFromAlternativeSources()                                │
└─────────────────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│               EXTERNAL DATA SOURCES                              │
│                                                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────┐│
│  │  Tarsheed API    │  │  OpenChargeMap   │  │  KAHRAMAA      ││
│  │  (To be         │  │  (Alternative)   │  │  (Official)    ││
│  │   discovered)   │  │                  │  │                ││
│  └──────────────────┘  └──────────────────┘  └────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. User Views Charging Stations

```
User → /charging Page
         │
         ├─→ Load Google Maps
         │
         ├─→ Get User Location (Geolocation API)
         │
         ├─→ Fetch Stations (Supabase)
         │    SELECT * FROM charging_stations
         │    WHERE status = 'active'
         │
         └─→ Render Map + Markers
```

### 2. Data Sync Process

```
Cron Job / Manual Trigger
         │
         ▼
Sync Service (charging-sync.ts)
         │
         ├─→ scrapeTarsheedData()
         │    │
         │    ├─→ Tarsheed API (future)
         │    └─→ Mock Data (current)
         │
         ├─→ Transform Data
         │    │
         │    └─→ Map to database schema
         │
         └─→ Upsert to Database
              │
              └─→ charging_stations table
```

### 3. Real-time Updates

```
Database Change
         │
         ▼
Supabase Realtime
         │
         ├─→ WebSocket Broadcast
         │
         └─→ Client Receives Update
              │
              ├─→ Update Map Markers
              └─→ Update Station Cards
```

## Component Architecture

### Charging Page Component Tree

```
page.tsx (ChargingPage)
  │
  ├─→ useState
  │    ├─ stations
  │    ├─ selectedStation
  │    ├─ userLocation
  │    └─ filter
  │
  ├─→ useEffect
  │    ├─ fetchChargingStations()
  │    └─ getUserLocation()
  │
  ├─→ <LoadScript> (Google Maps)
  │    │
  │    └─→ <GoogleMap>
  │         │
  │         ├─→ <Marker> (User Location)
  │         │
  │         ├─→ <Marker>[] (Stations)
  │         │
  │         └─→ <InfoWindow> (Selected Station)
  │
  └─→ Station Cards Grid
       │
       └─→ StationCard[] (map over filtered stations)
```

## Database Schema Diagram

```
┌─────────────────────────┐
│      profiles           │
│ ─────────────────────── │
│ id (PK)                 │
│ email                   │
│ full_name               │
│ role                    │
└─────────────────────────┘
            │ 1
            │
            │ N
┌─────────────────────────┐         ┌─────────────────────────┐
│  charging_sessions      │    N:1  │  charging_stations      │
│ ─────────────────────── │ ────────│ ─────────────────────── │
│ id (PK)                 │         │ id (PK)                 │
│ user_id (FK)            │         │ name                    │
│ station_id (FK) ────────┼────────→│ address                 │
│ vehicle_id (FK)         │         │ latitude                │
│ start_time              │         │ longitude               │
│ end_time                │         │ provider                │
│ energy_delivered_kwh    │         │ charger_type            │
│ cost_qar                │         │ power_output_kw         │
│ status                  │         │ total_chargers          │
│ notes                   │         │ available_chargers      │
└─────────────────────────┘         │ status                  │
            │ N                      │ operating_hours         │
            │                        │ pricing_info            │
            │ 1                      │ amenities[]             │
┌─────────────────────────┐         │ last_scraped_at         │
│       vehicles          │         └─────────────────────────┘
│ ─────────────────────── │
│ id (PK)                 │
│ manufacturer            │
│ model                   │
│ range_km                │
└─────────────────────────┘
```

## API Endpoints

```
GET  /api/sync-stations
     Returns: { success: true, lastSync: "2026-01-01T12:00:00Z" }
     
POST /api/sync-stations
     Triggers: Data sync from Tarsheed
     Returns: { success: true, count: 15, results: [...] }
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         PRODUCTION                               │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐    ┌──────────────┐    ┌──────────────────────┐
│   Vercel     │    │   Supabase   │    │   Google Cloud      │
│   (Next.js)  │───→│  (Database)  │    │   (Maps API)        │
│              │    │              │    │                      │
│  - Web App   │    │  - PostgreSQL│    │  - Maps JS API      │
│  - API Routes│    │  - Auth      │    │  - Geocoding        │
│  - SSR/SSG   │    │  - Realtime  │    │  - Directions       │
└──────────────┘    └──────────────┘    └──────────────────────┘
       │                    ▲                     ▲
       │                    │                     │
       │            ┌───────┴────────┐            │
       │            │                │            │
       ▼            ▼                ▼            │
┌──────────────┐  ┌──────────────┐  ┌────────────┴─────┐
│  Cron Job    │  │  Tarsheed    │  │  Browser Users   │
│  (Sync Data) │  │  API         │  │                  │
│              │  │  (External)  │  │  - View map      │
│  - Hourly    │  │              │  │  - Filter        │
│  - Updates   │  │              │  │  - Navigate      │
└──────────────┘  └──────────────┘  └──────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                      SECURITY ARCHITECTURE                       │
└─────────────────────────────────────────────────────────────────┘

1. Network Layer
   │
   ├─→ HTTPS/TLS (All traffic encrypted)
   └─→ CORS (Restrict origins)

2. Application Layer
   │
   ├─→ Environment Variables (Secrets)
   ├─→ Input Validation (TypeScript + Zod)
   └─→ Rate Limiting (API endpoints)

3. Database Layer
   │
   ├─→ Row Level Security (RLS)
   │    │
   │    ├─ charging_stations: Public read
   │    └─ charging_sessions: User-specific
   │
   ├─→ Authentication (Supabase Auth)
   └─→ Prepared Statements (SQL injection prevention)

4. API Layer
   │
   ├─→ API Key Restrictions (Google Maps)
   ├─→ Admin-only Endpoints (Sync service)
   └─→ Request Logging (Audit trail)
```

## Future Enhancements

```
Phase 1: Core Features (COMPLETE ✅)
├─ Database schema
├─ Google Maps integration
├─ Basic scraping infrastructure
├─ Web interface
└─ Documentation

Phase 2: Tarsheed Integration (IN PROGRESS 🔄)
├─ API discovery
├─ Real data scraping
├─ Automated sync
└─ Error handling

Phase 3: Advanced Features (PLANNED 📋)
├─ Real-time availability
├─ Charging session tracking
├─ Route planning
├─ Push notifications
├─ User reviews
├─ Favorites
└─ Booking system

Phase 4: Mobile Integration (PLANNED 📱)
├─ React Native app
├─ Offline maps
├─ Background location
└─ CarPlay/Android Auto

Phase 5: Analytics (PLANNED 📊)
├─ Usage analytics
├─ Popular stations
├─ Peak times
└─ Cost tracking
```

---

**This architecture is scalable, secure, and ready for production deployment!**
